<% if obj&.errors&.any? %>
  <div class="container bg-danger p-3 mb-4 text-light">
    <p><b>The following errors preventing the user from saving:</b></p>
    <ul>
      <% obj.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>