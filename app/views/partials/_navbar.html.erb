<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">
      <%= image_tag "cat_logo.png", class:"d-inline-block align-text-center", style: 'height:50px;width:50px;'%>
      Millian Library
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Books", books_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to "Members", members_path, class: 'nav-link' %>
        </li>
      </ul>
      <div class="d-flex text-center">
        <%= form_with(url: books_search_path, method: :get, local: true, class: 'd-flex me-2') do %>
          <%= text_field_tag :query, params[:query], class: 'form-control me-2', placeholder: 'Search books...' %>
          <%= submit_tag "Search", class: "btn btn-outline-primary" %>
        <% end %>

        <% if logged_in? %>
          <%= link_to current_user.name, member_path(current_user), class: 'p-1 mx-1 rounded-pill text-bg-primary text-decoration-none text-white' %>
          <%= link_to "Log Out", logout_path, class: "btn btn-info mx-1" %>
        <% else %>
          <%= link_to "Sign Up", signup_path, class: 'btn btn-info mx-1' %>
          <%= link_to "Login", login_path, class: 'btn btn-info mx-1' %>
        <% end %>
      </div>
    </div>
  </div>
</nav>